[pytest]
markers =
    suite(name): Test suite grouping (smoke, system, pipeline, clinical, etc.)
    level(name): Automation level (auto, semi, manual)
    security: Security-related tests (DoS, ports, TLS, auth)
